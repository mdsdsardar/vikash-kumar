pipeline{
    agent any 
    stages{
        stage("Github automation"){
            steps{    
                git branch: 'main', url: 'https://github.com/mdsdsardar/vikash-kumar.git'
            }
        }
        // tools {
        // maven 'apache-maven-3.8.6' 
        // }

        stage("Unit Testing"){
            tools {
                maven 'Maven' 
            }

            steps{    
                sh 'mvn test'
            }
            
        }
        stage("Integratin Testing"){
            tools {
                maven 'Maven' 
            }
            steps{    
                sh 'mvn verify -DskipUnitTests'
            }
        }
        stage("Maven Build"){
            tools {
                maven 'Maven' 
            }
            steps{    
                sh 'mvn clean install'
            }
        }
        stage("SonarQube analysis"){
            tools {
                maven 'Maven'
            }
            steps{ 
                script{
                    sh 'mvn clean package sonar:sonar'
                    mvn sonar:sonar -Dsonar.login=squ_9cb59b718c58823e86353a732fb04e2dbd197a6b
                        
                }   
                
            }
        }
        stage("Quality gate status"){
            steps{ 
                script{
                    waitForQualityGate abortPipeline: false, credentialsId: 'sonar-api'   
                }       
            }
        }
        // stage("Upload war file to nexus"){
        //     steps{ 
        //         script{
        //             sh 'mvn clean install'
                    
        //         }   
                
        //     }
        // }
    }
}
//if you dont know how to write pipeline syntax, goto pipeline syntax from there you can get the syntax.